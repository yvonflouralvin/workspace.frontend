services:
  app:
    build:
      context: ../..
      dockerfile: apps/hr/Dockerfile
    container_name: workspace_uat_ft_hr
    restart: unless-stopped
    env_file:
      - .env
    expose:
      - "3003"
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true
