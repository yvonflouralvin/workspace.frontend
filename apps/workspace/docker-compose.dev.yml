# docker-compose.dev.yml — app workspace seule, hot reload (next dev)
# Usage : docker compose -f docker-compose.dev.yml up
name: ft_workspace

services:
  ft_workspace:
    image: node:24-alpine
    container_name: dev_ft_workspace
    restart: unless-stopped
    working_dir: /app
    volumes:
      - ../../:/app
      - ../../node_modules:/app/node_modules
      - workspace_next:/app/apps/workspace/.next
    env_file: ./.env
    environment:
      PORT: "3005"
      HOSTNAME: "0.0.0.0"
      AUTH_API_URL: http://dev_auth:4999
      AUDIT_LOGS_API_URL: http://dev_audit_logs:5006
    command: sh -c "[ ! -d node_modules/.bin ] && npm ci; cd apps/workspace && exec npx next dev --port 3005"
    ports:
      - "3005:3005"
    networks:
      - dev

networks:
  dev:
    name: dev
    external: true

volumes:
  workspace_next:
