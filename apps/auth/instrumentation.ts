// Vars injectées via env_file docker-compose — plus de fetch vault.
// Réserver ce fichier pour l'injection de NEXT_PUBLIC au runtime (Server Components uniquement).
export async function register() {}
