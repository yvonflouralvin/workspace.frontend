# Copier en `.env` à côté de docker-compose.yml (jamais committer le .env réel).
# Landing page publique — pas de dépendance backend, un seul NEXT_PUBLIC_* : où
# pointer les boutons « Se connecter »/« Essayer gratuitement » (app auth).

APP_PORT=3004
NEXT_PUBLIC_AUTH_APP_URL=https://auth.saas.cd
